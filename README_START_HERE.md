# 🚀 เริ่มต้นใช้งาน - สำหรับผู้ที่ซื้อโค้ดมา

## ✅ ขั้นตอนที่ 1: ตรวจสอบระบบ

รันคำสั่งนี้เพื่อตรวจสอบว่าทุกอย่างพร้อมหรือยัง:

```powershell
npm run setup
```

คำสั่งนี้จะ:
- ✅ ตรวจสอบไฟล์ `.env`
- ✅ ตรวจสอบการตั้งค่า MongoDB Atlas
- ✅ ตรวจสอบการตั้งค่าฐานข้อมูลเดิม
- ✅ ทดสอบการเชื่อมต่อ Atlas
- ✅ แสดงคำแนะนำที่ต้องทำ

---

## 📋 สิ่งที่ต้องมี

### 1. MongoDB Atlas Connection String
คุณต้องมี connection string จาก MongoDB Atlas:
```
mongodb+srv://username:password@cluster.mongodb.net/api-factory
```

**วิธีหา:**
1. เข้า MongoDB Atlas
2. ไปที่ Clusters → Connect
3. เลือก "Connect your application"
4. คัดลอก connection string

### 2. ฐานข้อมูลเดิม (ถ้าต้องการ migrate)
ถ้าคุณต้องการ migrate ข้อมูลจากฐานข้อมูลเดิม คุณต้องมี:
- Connection string ของฐานข้อมูลเดิม
- หรือถ้าไม่มีฐานข้อมูลเดิม ก็ไม่ต้อง migrate

---

## 🔧 ขั้นตอนการตั้งค่า

### ขั้นตอนที่ 1: ตั้งค่า MongoDB Atlas

1. เปิดไฟล์ `.env` (ถ้าไม่มีจะสร้างให้)
2. เพิ่มบรรทัดนี้:
   ```
   MONGODB_URL=mongodb+srv://username:password@cluster.mongodb.net/api-factory?retryWrites=true&w=majority
   ```
   (แทนที่ username, password, cluster ด้วยค่าจริงของคุณ)

### ขั้นตอนที่ 2: ตั้งค่าฐานข้อมูลเดิม (ถ้ามี)

ถ้าคุณมีฐานข้อมูลเดิมที่ต้องการ migrate:

1. เปิดไฟล์ `.env`
2. เพิ่มบรรทัดนี้:
   ```
   MONGODB_SOURCE_URL=mongodb://localhost:27017/api-factory
   ```
   (หรือ connection string ของฐานข้อมูลเดิมของคุณ)

### ขั้นตอนที่ 3: ตรวจสอบอีกครั้ง

```powershell
npm run setup
```

---

## 🚀 วิธีใช้งาน

### ถ้าไม่ต้องการ migrate (ใช้ Atlas ใหม่เลย):

1. ตั้งค่า `MONGODB_URL` ใน `.env` (ดูขั้นตอนที่ 1)
2. ตรวจสอบ: `npm run setup`
3. เริ่มใช้งาน: `npm start`

### ถ้าต้องการ migrate ข้อมูล:

1. ตั้งค่า `MONGODB_URL` และ `MONGODB_SOURCE_URL` ใน `.env`
2. ตรวจสอบ: `npm run setup`
3. Migrate: `npm run migrate-to-atlas`
4. ตรวจสอบผลลัพธ์: `npm run check-db`
5. เริ่มใช้งาน: `npm start`

---

## 📚 คำสั่งที่มีให้ใช้

| คำสั่ง | คำอธิบาย |
|--------|----------|
| `npm run setup` | ตรวจสอบและแสดงสถานะระบบ |
| `npm run check-db` | ตรวจสอบการเชื่อมต่อฐานข้อมูล |
| `npm run migrate-to-atlas` | Migrate ข้อมูลไปยัง Atlas |
| `npm start` | เริ่มแอปพลิเคชัน |

---

## ⚠️ ปัญหาที่พบบ่อย

### 1. Error: "ECONNREFUSED"
**สาเหตุ:** ไม่สามารถเชื่อมต่อกับฐานข้อมูลได้

**วิธีแก้:**
- ตรวจสอบ connection string ถูกต้องหรือไม่
- ตรวจสอบ Network Access ใน Atlas (เพิ่ม IP address)
- ตรวจสอบว่า MongoDB service ทำงานอยู่ (ถ้าใช้ localhost)

### 2. Error: "Authentication failed"
**สาเหตุ:** Username/Password ไม่ถูกต้อง

**วิธีแก้:**
- ตรวจสอบ username/password ใน connection string
- ตรวจสอบว่า user มีสิทธิ์เข้าถึง database

### 3. ไม่พบไฟล์ .env
**วิธีแก้:**
- สร้างไฟล์ `.env` ในโฟลเดอร์ root ของโปรเจกต์
- เพิ่ม `MONGODB_URL` และ `MONGODB_SOURCE_URL` (ถ้ามี)

---

## 💡 คำแนะนำ

1. **เริ่มจาก `npm run setup`** - จะบอกว่าต้องทำอะไรบ้าง
2. **อ่าน error message** - มักจะมีคำแนะนำอยู่ใน error
3. **ตรวจสอบ Network Access** - สำคัญมากสำหรับ Atlas
4. **ใช้ `npm run check-db`** - เพื่อตรวจสอบการเชื่อมต่อ

---

## 📞 ต้องการความช่วยเหลือ?

1. รัน `npm run setup` และดูผลลัพธ์
2. อ่านไฟล์คู่มือ:
   - `README_MIGRATE.md` - คู่มือการ migrate
   - `QUICK_MIGRATE.md` - คู่มือแบบย่อ
   - `FIX_MIGRATE.md` - แก้ไขปัญหา

---

## ✅ พร้อมใช้งานเมื่อ:

- ✅ รัน `npm run setup` แล้วไม่มี error
- ✅ `npm run check-db` เชื่อมต่อได้
- ✅ สามารถรัน `npm start` ได้

**เริ่มต้นด้วย:** `npm run setup` 🚀
