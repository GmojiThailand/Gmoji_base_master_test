### Example environment template for local testing
# Do NOT commit real credentials; this is only an example.

# Preferred (Railway / Production): a complete connection string
# MONGODB_URL=mongodb://mongo:YOUR_PASSWORD@turntable.proxy.rlwy.net:46691/api-factory?authSource=admin

# Fallback parts (if you need to construct one manually)
MONGOUSER=mongo
MONGOPASSWORD=your_password_here
MONGOHOST=turntable.proxy.rlwy.net
MONGOPORT=46691
MONGO_DB=api-factory
MONGO_AUTH_SOURCE=admin

# Optional: Mongo internal host (for services inside Railway project)
# MONGO_URL=mongodb://mongo:YOUR_PASSWORD@mongodb.railway.internal:27017/api-factory?authSource=admin
# Example env file for local testing
# Replace values with your Railway-provided connection strings

# Preferred: full connection string
MONGODB_URL=

# Railway internal (use when running inside the same project/Network)
MONGO_URL=

# Railway public proxy (use when connecting from outside Railway)
MONGO_PUBLIC_URL=

# Other useful variables
NODE_ENV=development
PORT=3000
